namespace Info360.Models;

public class Restriccion
{
    public string restriccion { get; private set; }

    public Restriccion(string restriccion)
    {
        this.restriccion = restriccion;
    }
}